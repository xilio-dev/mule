<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stackoka.stackoka.repository.column.ColumnMapper">

    <select id="selectColumnsByArticleId" resultType="com.stackoka.stackoka.common.data.column.Column">
         select c.id,c.name,c.user_id as userId, c.cover,c.intro from columns c
         inner join article_column ac on ac.column_id=c.id where c.user_id=#{userId} and ac.article_id=#{articleId}
    </select>
</mapper>
